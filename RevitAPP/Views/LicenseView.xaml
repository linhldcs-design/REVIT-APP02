<Window x:Class="RevitAPP.Views.LicenseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="License - RevitAPP"
        Width="460" SizeToContent="Height"
        MinWidth="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.Background}"
        FontFamily="{DynamicResource Font.Family}"
        FontSize="{DynamicResource Font.Size.Body}"
        Foreground="{DynamicResource Brush.Text}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/RevitAPP;component/Resources/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="{DynamicResource Spacing.Large}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Kich hoat cong cu ve thep"
                       FontSize="{DynamicResource Font.Size.Header}"
                       Foreground="{DynamicResource Brush.Header}"/>
            <TextBlock Text="Dang nhap bang tai khoan Google da duoc cap quyen de su dung cac lenh ve thep."
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource Brush.TextSecondary}"
                       Margin="0,6,0,0"/>
        </StackPanel>

        <Border Grid.Row="1"
                Background="{DynamicResource Brush.Surface}"
                BorderBrush="{DynamicResource Brush.Border}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="14"
                Margin="0,14,0,14">
            <TextBlock Text="{Binding StatusText}"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource Brush.Text}"/>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Dang xuat"
                    Style="{StaticResource Button.Secondary}"
                    Command="{Binding SignOutCommand}"
                    IsEnabled="{Binding IsSignedIn}"
                    MinWidth="110" Margin="0,0,10,0"/>
            <Button Content="Dang nhap Google"
                    Style="{StaticResource Button.Primary}"
                    Command="{Binding SignInCommand}"
                    IsEnabled="{Binding IsNotBusy}"
                    MinWidth="150"/>
        </StackPanel>
    </Grid>
</Window>
